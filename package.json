{
  "name": "curolia",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/connectors/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:status": "supabase status",
    "db:reset": "supabase db reset",
    "db:migrate": "supabase migration up",
    "db:types": "supabase gen types typescript --local > apps/web/src/lib/database.types.ts",
    "functions:sync": "node scripts/sync-connector-supabase.mjs",
    "functions:start": "supabase functions serve",
    "functions:stop": "pkill -f 'supabase functions serve' || true"
  },
  "devDependencies": {
    "supabase": "^2.98.0",
    "turbo": "^2.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
