/** Stable ids and labels shared by manifest and trace UI (single source of truth). */
export const googlePhotosPluginMeta = {
  typeId: "google_photos" as const,
  displayName: "Google Photos",
  implemented: true as const,
};
